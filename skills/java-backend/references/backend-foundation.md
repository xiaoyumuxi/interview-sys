# Backend Foundation Reference

- HTTP API 设计要关注资源建模、状态码、错误码、分页、幂等键、鉴权和版本兼容。
- 后端可靠性要覆盖超时、重试、熔断、限流、降级、隔离、补偿和告警。
- 异步任务要追问投递可靠性、重复消费、顺序性、失败重试、死信队列和任务幂等。
- 可观测性要包含日志、指标、链路追踪、业务事件和排障 runbook。
- 安全基础要覆盖认证、授权、敏感数据脱敏、输入校验、SQL 注入和越权访问。
